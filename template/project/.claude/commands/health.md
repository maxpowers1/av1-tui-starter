Run the `framework-health` subagent to check whether the framework itself
is being maintained properly: context budget, decision log quality, ADR
completeness, handoff freshness, structural integrity, and anti-patterns.

This checks the framework, not the application code. Use `/qa` for code quality.
